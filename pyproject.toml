[project]
name = "housing-price-prediction"
version = "1.0.0"
description = "Multi-model US housing price forecasting web app"
requires-python = ">=3.10"

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
addopts = "-q"
